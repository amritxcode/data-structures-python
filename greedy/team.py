class Solution():
    def write(self, n:int) -> int:
        implement = 0
        
        for _ in range(view):
            sure = map(int, input().split())
            row_sum = 0

            for num in sure:
                if num == 1:
                    row_sum += 1

            if row_sum > 1:
                implement += 1

        return implement

view = int(input())

# print(Solution().write(view))